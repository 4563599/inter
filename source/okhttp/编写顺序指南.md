# OkHttp 源码编写顺序指南

## 📋 总体编写顺序

```
第一阶段：基础数据类（4个类）
  ↓
第二阶段：接口定义（2个接口）
  ↓
第三阶段：核心实现（3个类）
  ↓
第四阶段：拦截器实现（4个类）
  ↓
第五阶段：连接和调度（3个类）
  ↓
第六阶段：示例代码（6个示例）
```

---

## 🎯 第一阶段：基础数据类（约2小时）

### 为什么先写这些？
- ✅ 这些是最基础的数据结构
- ✅ 没有复杂的逻辑，容易理解
- ✅ 后面所有类都依赖这些基础类
- ✅ 熟悉 Builder 模式

---

### 1. RequestBody.java（30分钟）

**为什么第一个写？**
- 最简单，只是一个抽象类
- 理解请求体的概念
- 学习工厂方法模式

**编写要点：**
```java
/**
 * 请求体抽象类
 * 
 * 设计思路：
 * 1. 使用抽象类而不是接口：因为需要提供默认实现
 * 2. 支持多种类型：JSON、表单、文件等
 * 3. 延迟读取：不在构造时读取，而是在写入时读取
 */
```

**关键方法：**
- `contentType()` - 返回 MIME 类型
- `contentLength()` - 返回内容长度（如果未知返回-1）
- `writeTo(OutputStream)` - 写入数据到输出流

---

### 2. ResponseBody.java（30分钟）

**为什么第二个写？**
- 与 RequestBody 对应，逻辑相似
- 理解响应体的概念
- 学习资源管理（Closeable）

**编写要点：**
```java
/**
 * 响应体抽象类
 * 
 * 设计思路：
 * 1. 实现 Closeable：确保资源正确关闭
 * 2. 提供多种读取方式：string(), bytes(), byteStream()
 * 3. 只能读取一次：流一旦读取就关闭
 */
```

**关键方法：**
- `byteStream()` - 返回输入流
- `string()` - 读取为字符串（会关闭流）
- `bytes()` - 读取为字节数组（会关闭流）
- `close()` - 关闭资源

---

### 3. Request.java（45分钟）

**为什么第三个写？**
- 理解 Builder 模式的完整实现
- 学习不可变对象的设计
- 这是用户最常接触的类

**编写要点：**
```java
/**
 * HTTP 请求对象
 * 
 * 设计思路：
 * 1. 不可变对象：所有字段都是 final
 * 2. Builder 模式：提供灵活的构建方式
 * 3. 链式调用：每个方法返回 Builder 自身
 * 4. 私有构造函数：只能通过 Builder 创建
 * 
 * 为什么设计成不可变？
 * - 线程安全：多个线程可以安全共享
 * - 防止意外修改：拦截器之间传递时不会被改变
 * - 简化设计：不需要考虑状态变化
 */
```

**关键设计：**
```java
// 1. 所有字段 final
private final String url;
private final String method;

// 2. 私有构造函数
private Request(Builder builder) { ... }

// 3. Builder 内部类
public static class Builder {
    // 链式调用
    public Builder url(String url) {
        this.url = url;
        return this;  // 返回自身
    }
}

// 4. 提供 newBuilder() 用于修改
public Builder newBuilder() {
    return new Builder(this);
}
```

---

### 4. Response.java（45分钟）

**为什么第四个写？**
- 与 Request 对应
- 理解请求-响应模型
- 包含更复杂的设计（priorResponse）

**编写要点：**
```java
/**
 * HTTP 响应对象
 * 
 * 设计思路：
 * 1. 包含对 Request 的引用：知道这个响应对应哪个请求
 * 2. priorResponse：支持重定向链
 * 3. isSuccessful()：方便判断成功与否
 * 
 * 为什么需要 priorResponse？
 * - 记录重定向历史
 * - 调试时可以看到完整的重定向链
 * - 例如：请求A → 301 → 请求B → 200
 */
```

**关键设计：**
```java
// 重定向链的设计
private final Response priorResponse;  // 前一个响应

// 方便的判断方法
public boolean isSuccessful() {
    return code >= 200 && code < 300;
}
```

---

## 🎯 第二阶段：接口定义（约1小时）

### 为什么现在写接口？
- ✅ 定义契约，明确职责
- ✅ 后面的实现类需要实现这些接口
- ✅ 理解面向接口编程

---

### 5. Call.java（30分钟）

**为什么先写这个接口？**
- 定义请求执行的契约
- 理解同步和异步的区别
- 包含内部接口 Callback

**编写要点：**
```java
/**
 * Call 接口：表示一个准备执行的请求
 * 
 * 设计思路：
 * 1. 每个 Call 只能执行一次：防止重复执行
 * 2. 支持同步和异步：execute() 和 enqueue()
 * 3. 可以取消：cancel()
 * 4. 可以克隆：clone() 用于重新执行
 * 
 * 为什么只能执行一次？
 * - 执行过程中可能会修改内部状态
 * - 避免并发问题
 * - 如果需要重复执行，使用 clone()
 */
```

**关键方法：**
```java
// 同步执行：阻塞当前线程
Response execute() throws IOException;

// 异步执行：提交到线程池
void enqueue(Callback callback);

// 取消请求
void cancel();

// 检查状态
boolean isExecuted();
boolean isCanceled();

// 克隆（用于重新执行）
Call clone();

// 回调接口
interface Callback {
    void onFailure(Call call, IOException e);
    void onResponse(Call call, Response response);
}
```

---

### 6. Interceptor.java（30分钟）

**为什么写这个接口？**
- 这是整个 OkHttp 最核心的设计
- 定义拦截器的契约
- 理解责任链模式

**编写要点：**
```java
/**
 * 拦截器接口 - OkHttp 最核心的设计
 * 
 * 设计思路：
 * 1. 责任链模式：每个拦截器处理后传给下一个
 * 2. 双向处理：可以处理请求，也可以处理响应
 * 3. 灵活性：可以修改、替换、短路请求
 * 
 * 拦截器能做什么？
 * - 修改请求：添加请求头、修改 URL
 * - 短路请求：直接返回缓存，不继续执行
 * - 修改响应：解压 gzip、添加响应头
 * - 记录日志：记录请求耗时、数据量
 * 
 * Chain 接口的作用：
 * - 持有当前请求
 * - 提供 proceed() 方法继续执行链
 * - 提供配置信息（超时等）
 */
```

**关键设计：**
```java
interface Interceptor {
    // 拦截方法 - 唯一的方法
    Response intercept(Chain chain) throws IOException;
    
    // Chain 接口 - 拦截器链
    interface Chain {
        Request request();           // 当前请求
        Response proceed(Request r); // 继续执行下一个拦截器
        OkHttpClient client();       // 获取客户端
        int connectTimeoutMillis();  // 连接超时
        int readTimeoutMillis();     // 读取超时
        int writeTimeoutMillis();    // 写入超时
    }
}

// 典型的拦截器实现
class MyInterceptor implements Interceptor {
    @Override
    public Response intercept(Chain chain) throws IOException {
        Request request = chain.request();
        
        // 1. 请求前处理
        System.out.println("发送请求: " + request.url());
        
        // 2. 继续执行链
        Response response = chain.proceed(request);
        
        // 3. 响应后处理
        System.out.println("收到响应: " + response.code());
        
        return response;
    }
}
```

---

## 🎯 第三阶段：核心实现（约3小时）

### 为什么现在写核心实现？
- ✅ 前面的基础已经打好
- ✅ 现在可以实现完整的逻辑
- ✅ 这是最复杂也是最关键的部分

---

### 7. RealInterceptorChain.java（1小时）⭐⭐⭐⭐⭐

**为什么先写这个？**
- **这是整个 OkHttp 最核心的类！**
- 实现责任链模式的关键
- 理解递归调用
- 后面的 RealCall 需要用到

**编写要点：**
```java
/**
 * 拦截器链的真实实现 - 最核心的类！
 * 
 * 设计思路（重要！）：
 * 1. 维护拦截器列表和当前索引
 * 2. proceed() 创建新的 Chain（索引+1）
 * 3. 递归调用形成责任链
 * 4. 防止拦截器多次调用 proceed()
 * 
 * 执行流程（仔细理解）：
 * 
 * proceed(index=0)
 *   → interceptor[0].intercept(chain_1)
 *       → chain_1.proceed(index=1)
 *           → interceptor[1].intercept(chain_2)
 *               → chain_2.proceed(index=2)
 *                   → ... 
 *                       → 返回 Response
 *                   ← 处理 Response
 *               ← 返回 Response
 *           ← 处理 Response
 *       ← 返回 Response
 * 
 * 关键点：
 * - 每次 proceed() 都创建新的 Chain 对象
 * - index 递增，指向下一个拦截器
 * - 形成递归调用
 * - 请求向内传递，响应向外返回
 */
```

**核心实现：**
```java
public class RealInterceptorChain implements Interceptor.Chain {
    private final List<Interceptor> interceptors;  // 拦截器列表
    private final int index;                       // 当前索引
    private final Request request;                 // 当前请求
    private int calls;                             // 调用次数（防止多次调用）
    
    @Override
    public Response proceed(Request request) throws IOException {
        // 1. 检查索引（是否已到达末尾）
        if (index >= interceptors.size()) {
            throw new AssertionError("拦截器列表已执行完毕");
        }
        
        // 2. 检查调用次数（防止多次调用）
        calls++;
        if (calls > 1) {
            throw new IllegalStateException("不能多次调用 proceed()");
        }
        
        // 3. 创建下一个 Chain（关键！）
        RealInterceptorChain next = new RealInterceptorChain(
            interceptors,
            index + 1,  // 索引+1，指向下一个拦截器
            request,
            ...
        );
        
        // 4. 获取当前拦截器
        Interceptor interceptor = interceptors.get(index);
        
        // 5. 执行拦截器（递归调用的关键！）
        Response response = interceptor.intercept(next);
        
        // 6. 检查响应
        if (response == null) {
            throw new NullPointerException("拦截器返回了 null");
        }
        
        return response;
    }
}
```

**为什么这样设计？**
- 每次创建新的 Chain：保持不可变性，线程安全
- index 递增：自动移动到下一个拦截器
- calls 计数：防止拦截器错误地多次调用 proceed()

---

### 8. OkHttpClient.java（45分钟）

**为什么现在写这个？**
- 管理所有配置和资源
- 提供创建 Call 的工厂方法
- 完整的 Builder 模式示例

**编写要点：**
```java
/**
 * OkHttpClient - HTTP 客户端
 * 
 * 设计思路：
 * 1. 单例模式：通常一个应用只需要一个实例
 * 2. Builder 模式：灵活配置
 * 3. 工厂模式：创建 Call 对象
 * 4. 资源管理：管理连接池、线程池
 * 
 * 为什么推荐单例？
 * - 共享连接池：提升性能
 * - 共享线程池：节省资源
 * - 配置一致：避免配置不一致导致的问题
 * 
 * Builder 的作用：
 * - 配置超时时间
 * - 添加拦截器
 * - 设置连接池
 * - 等等...
 */
```

**关键设计：**
```java
public class OkHttpClient {
    // 核心资源（共享）
    private final Dispatcher dispatcher;
    private final ConnectionPool connectionPool;
    private final List<Interceptor> interceptors;
    
    // 配置（不可变）
    private final int connectTimeout;
    private final int readTimeout;
    
    // 私有构造函数
    private OkHttpClient(Builder builder) {
        this.dispatcher = builder.dispatcher;
        this.connectionPool = builder.connectionPool;
        // ... 复制所有配置
    }
    
    // 工厂方法：创建 Call
    public Call newCall(Request request) {
        return new RealCall(this, request);
    }
    
    // Builder 类
    public static class Builder {
        // 设置默认值
        private int connectTimeout = 10_000;
        
        public Builder connectTimeout(int timeout) {
            this.connectTimeout = timeout;
            return this;  // 链式调用
        }
        
        public OkHttpClient build() {
            return new OkHttpClient(this);
        }
    }
}
```

---

### 9. RealCall.java（1小时15分钟）⭐⭐⭐⭐⭐

**为什么最后写这个？**
- 需要用到 OkHttpClient
- 需要构建 RealInterceptorChain
- 包含同步和异步逻辑
- 这是连接用户和拦截器链的桥梁

**编写要点：**
```java
/**
 * Call 的真实实现
 * 
 * 设计思路：
 * 1. 管理执行状态：executed, canceled
 * 2. 构建拦截器链：按顺序添加拦截器
 * 3. 同步执行：execute() 直接调用
 * 4. 异步执行：enqueue() 提交到 Dispatcher
 * 5. 每个 Call 只能执行一次
 * 
 * 为什么有两个执行方法？
 * - execute()：阻塞当前线程，适合后台线程
 * - enqueue()：不阻塞，适合 UI 线程
 * 
 * 拦截器的顺序（重要！）：
 * 1. Application Interceptors（用户添加）
 * 2. RetryAndFollowUpInterceptor（重试和重定向）
 * 3. BridgeInterceptor（补充 HTTP 头）
 * 4. CacheInterceptor（缓存）
 * 5. ConnectInterceptor（建立连接）
 * 6. Network Interceptors（用户添加）
 * 7. CallServerInterceptor（真实网络请求）
 */
```

**核心实现：**
```java
public class RealCall implements Call {
    private final OkHttpClient client;
    private final Request originalRequest;
    private boolean executed;  // 是否已执行
    
    @Override
    public Response execute() throws IOException {
        // 1. 检查是否已执行
        synchronized (this) {
            if (executed) throw new IllegalStateException("已执行");
            executed = true;
        }
        
        try {
            // 2. 通知 Dispatcher
            client.dispatcher().executed(this);
            
            // 3. 执行请求（核心！）
            return getResponseWithInterceptorChain();
        } finally {
            // 4. 通知完成
            client.dispatcher().finished(this);
        }
    }
    
    // 构建拦截器链（关键方法！）
    private Response getResponseWithInterceptorChain() {
        List<Interceptor> interceptors = new ArrayList<>();
        
        // 按顺序添加拦截器（顺序很重要！）
        interceptors.addAll(client.interceptors());        // 用户拦截器
        interceptors.add(new RetryAndFollowUpInterceptor()); // 重试
        interceptors.add(new BridgeInterceptor());           // 补充头
        interceptors.add(new ConnectInterceptor());          // 连接
        interceptors.add(new CallServerInterceptor());       // 网络请求
        
        // 创建链并执行
        Interceptor.Chain chain = new RealInterceptorChain(
            interceptors, 0, originalRequest, client, ...
        );
        
        return chain.proceed(originalRequest);
    }
}
```

---

## 🎯 第四阶段：拦截器实现（约4小时）

### 为什么现在写拦截器？
- ✅ 框架已经搭好
- ✅ 可以专注于各个拦截器的逻辑
- ✅ 按照执行顺序编写

---

### 10. BridgeInterceptor.java（45分钟）

**为什么先写这个？**
- 逻辑最简单
- 只是添加 HTTP 头
- 容易理解和测试

**编写要点：**
```java
/**
 * 桥接拦截器 - 连接应用层和网络层
 * 
 * 职责：
 * 1. 补充必要的 HTTP 请求头
 * 2. 处理 Content-Type 和 Content-Length
 * 3. 添加 gzip 支持
 * 4. 添加 Keep-Alive
 * 
 * 为什么叫"桥接"？
 * - 应用层：用户构建的简单 Request
 * - 网络层：符合 HTTP 规范的完整请求
 * - 这个拦截器就是中间的"桥梁"
 * 
 * 需要补充哪些头？
 * - Host: 主机名
 * - Content-Type: 请求体类型
 * - Content-Length: 请求体长度
 * - User-Agent: 客户端标识
 * - Accept-Encoding: gzip（支持压缩）
 * - Connection: Keep-Alive（支持连接复用）
 */
```

---

### 11. RetryAndFollowUpInterceptor.java（1小时）

**为什么第二个写？**
- 需要处理重试和重定向
- 包含循环逻辑
- 理解 HTTP 状态码

**编写要点：**
```java
/**
 * 重试和重定向拦截器
 * 
 * 职责：
 * 1. 处理请求失败的重试
 * 2. 处理 3xx 重定向
 * 3. 限制重试和重定向次数
 * 
 * 重试策略：
 * - 超时：可以重试
 * - 连接失败：可以重试
 * - 4xx 客户端错误：不重试
 * - SSL 错误：不重试
 * 
 * 重定向处理：
 * - 301/302：改为 GET
 * - 307/308：保持原方法
 * - 最多重定向 20 次
 * 
 * 为什么用 while(true)？
 * - 需要循环处理重试和重定向
 * - 直到成功或达到最大次数
 */
```

---

### 12. ConnectInterceptor.java（1小时）

**为什么第三个写？**
- 需要用到连接池（还没写）
- 建立真实的网络连接
- 连接应用层和网络层

**编写要点：**
```java
/**
 * 连接拦截器 - 建立 TCP 连接
 * 
 * 职责：
 * 1. 从连接池获取可复用的连接
 * 2. 如果没有，创建新连接
 * 3. 使用完毕后，放回连接池
 * 
 * 连接复用的好处：
 * - 跳过 TCP 三次握手
 * - 跳过 TLS 握手（HTTPS）
 * - 显著提升性能
 * 
 * 什么情况可以复用连接？
 * - host 和 port 相同
 * - 连接未关闭
 * - 连接未被占用
 */
```

---

### 13. CallServerInterceptor.java（1小时15分钟）⭐⭐⭐⭐⭐

**为什么最后写？**
- 这是最底层的拦截器
- 执行真实的网络 I/O
- 需要理解 HTTP 协议
- 包含 Socket 编程

**编写要点：**
```java
/**
 * 服务器调用拦截器 - 执行真实的网络请求
 * 
 * 职责：
 * 1. 写入 HTTP 请求到 Socket
 * 2. 读取 HTTP 响应从 Socket
 * 3. 解析响应头和响应体
 * 
 * HTTP 请求格式：
 * GET /path HTTP/1.1\r\n
 * Host: example.com\r\n
 * User-Agent: MyOkHttp\r\n
 * \r\n
 * [请求体]
 * 
 * HTTP 响应格式：
 * HTTP/1.1 200 OK\r\n
 * Content-Type: text/html\r\n
 * Content-Length: 123\r\n
 * \r\n
 * [响应体]
 * 
 * 关键点：
 * - \r\n 是行分隔符（CRLF）
 * - 空行表示头部结束
 * - 根据 Content-Length 读取响应体
 */
```

---

## 🎯 第五阶段：连接和调度（约3小时）

### 14. RealConnection.java（45分钟）

**为什么先写这个？**
- ConnectionPool 需要用到
- 封装 Socket 连接
- 管理连接状态

**编写要点：**
```java
/**
 * 真实的 TCP 连接
 * 
 * 设计思路：
 * 1. 封装 Socket
 * 2. 引用计数：管理连接的使用状态
 * 3. 空闲时间：用于连接池清理
 * 
 * 引用计数的作用：
 * - 记录有多少请求正在使用此连接
 * - 0 表示空闲，可以复用或清理
 * - >0 表示使用中，不能关闭
 * 
 * 连接的生命周期：
 * 1. 创建：new RealConnection()
 * 2. 连接：connect() - 建立 TCP 连接
 * 3. 使用：acquire() - 引用计数+1
 * 4. 释放：release() - 引用计数-1
 * 5. 关闭：close() - 关闭 Socket
 */
```

---

### 15. ConnectionPool.java（1小时15分钟）⭐⭐⭐⭐

**为什么第二个写？**
- 需要 RealConnection
- 实现连接复用的关键
- 包含后台清理线程

**编写要点：**
```java
/**
 * 连接池 - 管理和复用 HTTP 连接
 * 
 * 设计思路：
 * 1. 维护连接列表
 * 2. get()：查找可复用的连接
 * 3. put()：放回连接
 * 4. cleanup()：清理空闲连接
 * 5. 后台线程：定期清理
 * 
 * 清理策略：
 * - 最多保持 5 个空闲连接
 * - 空闲连接保持 5 分钟
 * - 超过数量或时间，关闭最老的连接
 * 
 * 为什么需要后台线程？
 * - 定期检查空闲连接
 * - 自动清理过期连接
 * - 不需要手动管理
 * 
 * 如何判断连接可以复用？
 * 1. host 和 port 匹配
 * 2. 连接未关闭
 * 3. 连接未被占用（引用计数为0）
 */
```

---

### 16. Dispatcher.java（1小时）

**为什么最后写？**
- 管理异步请求
- 需要理解线程池
- RealCall 的异步执行需要用到

**编写要点：**
```java
/**
 * 调度器 - 管理异步请求的执行
 * 
 * 设计思路：
 * 1. 线程池：执行异步请求
 * 2. 运行队列：正在执行的请求
 * 3. 等待队列：等待执行的请求
 * 4. 并发控制：限制最大并发数
 * 
 * 为什么需要等待队列？
 * - 控制并发数量：避免资源耗尽
 * - 默认最多 64 个并发请求
 * - 超过限制的请求等待
 * 
 * 线程池配置：
 * - 核心线程数：0（按需创建）
 * - 最大线程数：无限制
 * - 空闲存活：60 秒
 * - 队列：SynchronousQueue（不缓存）
 * 
 * 为什么这样配置？
 * - 0 核心线程：节省资源
 * - 无限制最大线程：依靠 maxRequests 控制
 * - SynchronousQueue：直接交给线程执行
 */
```

---

## 🎯 第六阶段：示例代码（约2小时）

### 为什么最后写示例？
- ✅ 所有功能已经实现
- ✅ 可以验证代码是否正确
- ✅ 提供使用参考

### 编写顺序：
1. SimpleGetExample - 最简单的 GET
2. PostExample - POST 请求
3. AsyncExample - 异步请求
4. InterceptorExample - 自定义拦截器
5. ConnectionPoolExample - 连接池测试
6. RedirectExample - 重定向测试

---

## 📋 完整编写清单

复制这个清单，每完成一个就打勾：

```
第一阶段：基础数据类
[ ] 1. RequestBody.java
[ ] 2. ResponseBody.java
[ ] 3. Request.java
[ ] 4. Response.java

第二阶段：接口定义
[ ] 5. Call.java
[ ] 6. Interceptor.java

第三阶段：核心实现
[ ] 7. RealInterceptorChain.java ⭐⭐⭐⭐⭐
[ ] 8. OkHttpClient.java
[ ] 9. RealCall.java ⭐⭐⭐⭐⭐

第四阶段：拦截器实现
[ ] 10. BridgeInterceptor.java
[ ] 11. RetryAndFollowUpInterceptor.java
[ ] 12. ConnectInterceptor.java
[ ] 13. CallServerInterceptor.java ⭐⭐⭐⭐⭐

第五阶段：连接和调度
[ ] 14. RealConnection.java
[ ] 15. ConnectionPool.java ⭐⭐⭐⭐
[ ] 16. Dispatcher.java

第六阶段：示例代码
[ ] 17. SimpleGetExample.java
[ ] 18. PostExample.java
[ ] 19. AsyncExample.java
[ ] 20. InterceptorExample.java
[ ] 21. ConnectionPoolExample.java
[ ] 22. RedirectExample.java
```

---

## 💡 编写建议

### 每个类编写的步骤：

1. **先看原有代码**（5分钟）
   - 理解整体结构
   - 看注释和方法签名

2. **删除原代码，自己写**（主要时间）
   - 先写类注释（为什么需要这个类）
   - 再写字段（需要哪些数据）
   - 然后写方法（一个一个实现）
   - 每个方法都加注释

3. **对比差异**（10分钟）
   - 对比你的代码和原代码
   - 看看哪里不同
   - 理解为什么这样写

4. **运行测试**（5分钟）
   - 如果有示例，运行看看
   - 确保功能正确

### 遇到困难怎么办？

1. **先跳过，继续往后写**
   - 不要在一个地方卡太久
   - 后面可能就理解了

2. **画图帮助理解**
   - 画类图、流程图
   - 可视化更容易理解

3. **运行示例代码**
   - 看看实际效果
   - 打断点调试

4. **查看流程图**
   - 参考《核心流程图.md》
   - 理解执行流程

---

## 🎯 预计总时间

- 第一阶段：2 小时
- 第二阶段：1 小时
- 第三阶段：3 小时
- 第四阶段：4 小时
- 第五阶段：3 小时
- 第六阶段：2 小时

**总计：约 15 小时**

建议分 3-5 天完成，每天 3-5 小时。

---

**准备好了吗？开始你的编写之旅！** 🚀

